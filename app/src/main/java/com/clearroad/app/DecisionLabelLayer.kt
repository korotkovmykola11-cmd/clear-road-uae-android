package com.clearroad.app

/**
 * Stage 32.6 — decision label for the Home recommendation card.
 * Copy only; does not affect route selection or scoring.
 */
internal object DecisionLabelLayer {

    fun label(): String = "DECISION"
}
